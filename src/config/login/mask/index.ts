import { inube } from "@inubekit/inubekit"

const mask = {
    maskBackground: {
        backgroundColor: 'light',
        zIndex: 'behind'
    },
    maskModalWarning: {
        backgroundColor: 'dark',
        zIndex: 'frond'
    }
}

export { mask}