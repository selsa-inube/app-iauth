interface UserData {
  documentType: string;
  documentNumber: string;
  firstName: string;
  lastName: string;
  originatorId?: string;
  originatorCode?: string;
  originatorName?: string;
}

export type { UserData };
