import { inube } from "@inubekit/inubekit";

const mask = {
    light: {
        background: {
            color: inube.palette.neutral.N0
        }
    },
    dark: {
        background: {
            color: inube.palette.neutral.N900
        }
    }
};

export { mask };