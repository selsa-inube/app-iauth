import styled from "styled-components";

const StyledPasswordGrid = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(12rem, 1fr));
  gap: 1rem;
`;

export { StyledPasswordGrid };
