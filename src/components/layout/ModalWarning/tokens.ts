import { inube } from "@inubekit/inubekit";

const modalWarning = {
    background: {
        color: inube.palette.neutral.N0
    },
    boxShadow: {
        color: inube.palette.neutral.N10
    }
};

export { modalWarning };