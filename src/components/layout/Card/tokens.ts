import { inube } from "@inubekit/inubekit";

const card = {
    backgroundColor: {
        color: {
            light: inube.palette.neutral.N0
        }
    },
    boxShadow: {
        color: {
            dark: inube.palette.neutral.N10,
        }
    }
};

export { card }