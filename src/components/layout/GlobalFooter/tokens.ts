import { inube } from "@inubekit/inubekit";

// Fallback tokens for GlobalFooter when theme is not provided
const globalFooterTokens = {
  background: {
    color: inube.palette.neutral.N0,
  },
};

export { globalFooterTokens };
