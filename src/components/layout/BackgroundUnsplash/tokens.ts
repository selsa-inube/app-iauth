import { palette } from "@design/tokens/palette";

const backgroundUnsplashTokens = {
  mask: {
    background: {
      color: palette.neutralAlpha.W65A,
    },
  },
};

export { backgroundUnsplashTokens };
