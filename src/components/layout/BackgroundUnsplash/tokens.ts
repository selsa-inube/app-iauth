import { inube } from "@inubekit/inubekit";

const backgroundUnsplashTokens = {
  mask: {
    background: {
      color: inube.palette.neutralAlpha.N60A,
    },
  },
};

export { backgroundUnsplashTokens };
